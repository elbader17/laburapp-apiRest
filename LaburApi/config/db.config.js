//config a la base de datos
module.exports = {
    host: 'localhost', // aca ponemos la localhost, a menos q tengas la api corriendo en un server
    user: 'root', //el usuario x defecto es root
    password: '', //password puede q lleve o no
    database: 'laburappalpha' // nombre de la base de dato tienen que usar esta, que es la q yo les pase
    // port: 3306, el puerto es opcional dependiendo de la config q tienen en la coneixon 
};